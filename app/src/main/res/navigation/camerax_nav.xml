<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/camerax_nav"
    app:startDestination="@id/camera_permissions_fragment">
    <fragment
        android:id="@+id/camera_permissions_fragment"
        android:name="com.tomsky.androiddemo.fragment.CameraPermissionFragment"
        android:label="PermissionsFragment" >

        <action
            android:id="@+id/action_permissions_to_camera"
            app:destination="@id/camera_fragment"
            app:popUpTo="@id/camera_permissions_fragment"
            app:popUpToInclusive="true" />

    </fragment>

    <fragment
        android:id="@+id/camera_fragment"
        android:name="com.tomsky.androiddemo.fragment.CameraXFragment"
        android:label="CameraFragment" >

        <!--<action
            android:id="@+id/action_camera_to_gallery"
            app:destination="@id/gallery_fragment" />-->

        <action
            android:id="@+id/action_camera_to_permissions"
            app:destination="@id/camera_permissions_fragment"
            app:popUpTo="@id/camera_fragment"
            app:popUpToInclusive="true"/>

    </fragment>
</navigation>